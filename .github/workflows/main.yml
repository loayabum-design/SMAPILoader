name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Build Project
        run: dotnet build -c Release

      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: SMAPILoader-Output
          path: '**/bin/Release/**'
